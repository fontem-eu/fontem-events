# gmr-events

Shared Python lib: EventLog.emit(), EventConsumer base class, JSON Schema validation, Prometheus + Uptime Kuma helpers. Used by every producer (ETL loaders, consolidator) and every consumer (Virtuoso sink, Neo4j sink).